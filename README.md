# Advent of Code, in Clojure

This is an implementation of Advent of Code, in Clojure.

## Running

To run the app via the cli:

```bash
lein run [year]/day[day]

# For Example:
lein run 2015/day01
```

To start the web server:

```bash
lein ring server
```
